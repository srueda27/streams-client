//Instead of using the history object that ReactBrowser produce, we are going to create our own, much more easy to use it
import { createBrowserHistory } from 'history'; 
export default createBrowserHistory();